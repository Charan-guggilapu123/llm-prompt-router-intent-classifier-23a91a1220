{
  "name": "llm-prompt-router-intent-classifier",
  "version": "1.0.0",
  "description": "Intent-based prompt routing service with specialized AI personas",
  "private": true,
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "cli": "node src/app.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}